[theme]
primaryColor = "#3B82F6"              # Vibrant but not harsh blue (buttons, sliders)
backgroundColor = "#0B1220"           # Deep navy background
secondaryBackgroundColor = "#111827"  # Card/container background
textColor = "#E5E7EB"                 # Soft white (less eye strain than pure white)
font = "sans serif"